export default function MobileDashboard({
  session,
  profile,
  email,
  setEmail,
  password,
  setPassword,
  authMode,
  setAuthMode,
  loading,
  handleAuth,
  handleGoogleLogin,
  signOut,
}) {
